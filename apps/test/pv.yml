apiVersion: v1
kind: PersistentVolume
metadata:
  name: test-app-volume
  labels:
    name: test-app-volume
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 20Gi
  storageClassName: longhorn
